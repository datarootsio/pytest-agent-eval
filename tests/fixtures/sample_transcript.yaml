id: sample_booking
threshold: 1.0
runs: 1
tags:
  - gate:booking
turns:
  - user: "Book me a slot"
    expect:
      reply_contains_any:
        - confirmed
        - booked
      tool_calls_include:
        - book_slot
