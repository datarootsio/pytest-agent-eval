{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; [[ \"$f\" == *.py ]] && uvx ruff check --fix \"$f\" && uvx ruff format \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
