01-pytest-basics — replaces the console block in docs/why/what-is-a-test.md

  start:   docs/casts/rec.sh 01-pytest-basics
  window:  80x24 (set for you)

Plain pytest, no agent-eval: two node ids, one passing, one failing. Nothing about LLM
evaluation may appear on screen, so do not type anything not listed below. Both lines fit
inside 80 columns (68 and 71 characters with the prompt) and must not wrap.

TYPE:
  pytest -q tests/test_pricing.py::test_discount_applies_to_subtotal
      -> .                                                                        [100%]
      -> 1 passed in 0.00s
      -> the wall clock differs every take (0.00s–0.1s); only the verdict matters
      -> if this says "no tests ran" or "file or directory not found", the shell is not in
         the cast dir, or pyproject.toml is gone and pytest walked up to the repo root and
         inherited testpaths = ["tests"] — Ctrl-D and rerun rec.sh

  pytest -q tests/test_pricing.py::test_discount_dont_apply_to_subtotal
      -> F                                                                        [100%]
      -> =================================== FAILURES ===================================
      -> _____________________ test_discount_dont_apply_to_subtotal _____________________
      -> (blank)
      ->     def test_discount_dont_apply_to_subtotal():
      -> >       assert discount(subtotal=100, code="SPRINGS") == 90
      -> E       AssertionError: assert 100 == 90
      -> E        +  where 100 = discount(subtotal=100, code='SPRINGS')
      -> (blank)
      -> tests/test_pricing.py:12: AssertionError
      -> =========================== short test summary info ============================
      -> FAILED tests/test_pricing.py::test_discount_dont_apply_to_subtotal - Assertio...
      -> 1 failed in 0.10s
      -> the FAILED line is 101 characters and wraps onto a second row at 80 columns;
         it is one line of output, not two
      -> the FAILURES header must name _dont_apply_to_subtotal. If it names
         test_discount_applies_to_subtotal you re-ran the first node id — Ctrl-D and
         rerun rec.sh, because that mismatch is the exact defect this cast exists to fix

  Ctrl-D

THEN:
  docs/casts/upload.sh 01-pytest-basics
  paste the printed data-cast-id="..." into docs/why/what-is-a-test.md
