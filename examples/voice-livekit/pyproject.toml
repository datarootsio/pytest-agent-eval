[project]
name = "example-voice-livekit"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["pytest-agent-eval[livekit]"]

[tool.agent_eval]
yaml_dirs = ["evals"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
