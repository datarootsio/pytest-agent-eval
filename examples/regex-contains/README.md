# regex-contains

Deterministic reply assertions: substrings (`reply_contains_any`/`_all`) and regex patterns (`reply_matches_any`/`_all`, evaluated with `re.search`, case-insensitive).

```bash
pytest --agent-eval-live
```
