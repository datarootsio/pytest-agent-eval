# python-parametrize

The Python API: async test functions with `@pytest.mark.agent_eval`, the `agent_eval` fixture, and standard `@pytest.mark.parametrize` for programmatic test generation.

```bash
pytest --agent-eval-live
```

Requires `pytest-asyncio` (`asyncio_mode = "auto"` is set in pyproject.toml).
